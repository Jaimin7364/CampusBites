import { render, screen, waitFor } from '@testing-library/react'; import { beforeEach, describe, expect, it, vi } from 'vitest';
const service = vi.hoisted(() => ({ getAdminDashboard: vi.fn() })); vi.mock('@/services/admin-service', () => service); import { AdminDashboardView } from './admin-dashboard';
describe('AdminDashboardView', () => { beforeEach(() => vi.clearAllMocks()); it('shows tested platform metrics and paid completed value', async () => { service.getAdminDashboard.mockResolvedValue({ universities: 2, users: 8, sellers: 3, hotels: 4, pendingHotels: 1, approvedHotels: 3, featuredHotels: 2, orders: 12, pendingOrders: 2, completedOrders: 7, totalOrderValuePaise: 125050, totalOrderValueDefinition: 'Completed and paid orders only.' }); render(<AdminDashboardView/>); expect(await screen.findByText('₹1,250.50')).toBeInTheDocument(); expect(screen.getByText('Completed and paid orders only.')).toBeInTheDocument(); expect(screen.getByRole('link', { name: /Students 8/i })).toHaveAttribute('href', '/admin/users'); }); it('offers recovery after an API failure', async () => { service.getAdminDashboard.mockRejectedValue(new Error('offline')); render(<AdminDashboardView/>); expect(await screen.findByText('Platform statistics could not be loaded.')).toBeInTheDocument(); service.getAdminDashboard.mockResolvedValue({ universities: 0, users: 0, sellers: 0, hotels: 0, pendingHotels: 0, approvedHotels: 0, featuredHotels: 0, orders: 0, pendingOrders: 0, completedOrders: 0, totalOrderValuePaise: 0, totalOrderValueDefinition: 'Completed paid.' }); screen.getByRole('button', { name: 'Try again' }).click(); await waitFor(() => expect(screen.getByText('₹0.00')).toBeInTheDocument()); }); });
