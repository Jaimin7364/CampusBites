import { fireEvent, render, screen, waitFor } from '@testing-library/react'; import { beforeEach, describe, expect, it, vi } from 'vitest';
const service = vi.hoisted(() => ({ listAdminUsers: vi.fn(), getAdminUser: vi.fn(), listAdminSellers: vi.fn(), getAdminSeller: vi.fn() })); vi.mock('@/services/admin-service', () => service); import { AdminAccounts } from './admin-accounts';
const pagination = { page: 1, limit: 20, total: 1, totalPages: 1, hasNextPage: false, hasPreviousPage: false }; const student = { id: 'u1', role: 'USER', fullName: 'Riya Shah', sellerName: null, businessOwnerName: null, email: 'riya@example.com', phone: '9876543210', profilePhotoUrl: null, active: true, createdAt: '2026-08-21T00:00:00.000Z', updatedAt: '2026-08-21T00:00:00.000Z' };
describe('AdminAccounts', () => { beforeEach(() => vi.clearAllMocks()); it('searches students and opens safe detail', async () => { service.listAdminUsers.mockResolvedValue({ users: [student], pagination }); service.getAdminUser.mockResolvedValue({ user: { ...student, _count: { orders: 4 } } }); render(<AdminAccounts kind="users"/>); expect(await screen.findByText('Riya Shah')).toBeInTheDocument(); fireEvent.change(screen.getByLabelText('Search users'), { target: { value: 'Riya' } }); fireEvent.click(screen.getByRole('button', { name: 'Search' })); await waitFor(() => expect(service.listAdminUsers).toHaveBeenLastCalledWith(expect.objectContaining({ search: 'Riya' }))); fireEvent.click(screen.getByRole('button', { name: 'View details' })); expect(await screen.findByText('4')).toBeInTheDocument(); }); it('shows an empty seller state', async () => { service.listAdminSellers.mockResolvedValue({ sellers: [], pagination: { ...pagination, total: 0, totalPages: 0 } }); render(<AdminAccounts kind="sellers"/>); expect(await screen.findByText('No sellers found')).toBeInTheDocument(); }); });
