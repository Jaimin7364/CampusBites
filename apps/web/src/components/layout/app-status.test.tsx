import { act, fireEvent, render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { SESSION_EXPIRED_EVENT } from '@/services/api-client'; import { AppStatus } from './app-status';
describe('AppStatus', () => { it('announces offline recovery state', () => { Object.defineProperty(navigator, 'onLine', { configurable: true, value: false }); render(<AppStatus/>); expect(screen.getByText(/You are offline/)).toBeInTheDocument(); Object.defineProperty(navigator, 'onLine', { configurable: true, value: true }); act(() => window.dispatchEvent(new Event('online'))); expect(screen.queryByText(/You are offline/)).not.toBeInTheDocument(); }); it('announces and dismisses an expired session', () => { Object.defineProperty(navigator, 'onLine', { configurable: true, value: true }); render(<AppStatus/>); act(() => window.dispatchEvent(new Event(SESSION_EXPIRED_EVENT))); expect(screen.getByRole('alert')).toHaveTextContent('session expired'); fireEvent.click(screen.getByRole('button', { name: 'Dismiss' })); expect(screen.queryByRole('alert')).not.toBeInTheDocument(); }); });
